{
    "framework": null,
    "installCommand": "pip install -r requirements.txt",
    "buildCommand": "echo 'No build required'",
    "outputDirectory": ".",
    "devCommand": "python stock_analyzer.py",
    "routes": [
      {
        "src": "/api/(.*)",
        "dest": "stock_analyzer.py"
      }
    ]
  }